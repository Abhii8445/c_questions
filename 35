#include <stdio.h>
#include <stdlib.h>
int main(){
    int *ptr,i,n,l;
    ptr = (int*)malloc(10*sizeof(int));
    for (i = 0; i < 10; i++)
    {
        scanf("%d",&ptr[i]);
    }
    for (i = 0; i < 10; i++)
    {
        printf("the value of %d is %d",i,ptr[i]);
    }
    return 0;
}
